pre-commit:
  runner: bash
  commands:
    biome:
      run: pnpm biome check --write --staged
      stage_fixed: true
pre-push:
  runner: bash
  commands:
    typecheck:
      run: pnpm run typecheck
    build:
      run: pnpm run build
